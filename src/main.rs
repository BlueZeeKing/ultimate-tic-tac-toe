use dashmap::DashMap;
use ultimate_tic_tac_toe::{evalute, Board, IndividualBoard, Player};

fn main() {
    // let mut board = Board::default();
    // board = board.play(4, 0).unwrap();
    // let ((global, local), eval, depth) =
    //     board.minimax(6, &DashMap::new(), &DashMap::new(), &DashMap::new());
    // board = board.play(global, local).unwrap();
    // println!("{board}");
}
